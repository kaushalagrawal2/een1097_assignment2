fn main() {
    println!("EEN1097 Assignment 2 Code");
    println!("You should not be running this code, as the client and server code are in the same project.");
    println!("Use: cargo run --bin client");
    println!("And: cargo run --bin server");
    println!("Or use the `play` button in the VSCode client.rs and server.rs files over each main");
}
